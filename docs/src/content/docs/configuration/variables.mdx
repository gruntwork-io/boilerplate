---
title: Variables
sidebar:
  order: 2
description: Variable types, defaults, validations, and ordering.
---

import { Aside } from '@astrojs/starlight/components';

Variables are the primary way to parameterize your templates. They are defined in `boilerplate.yml` and their values
are provided interactively, via CLI flags, or from YAML files.

## Variable Definition

```yaml
variables:
  - name: ProjectName
    description: The name of the project
    type: string
    default: "my-project"
    order: 1
    validations:
      - required
      - length-3-50
```

### Fields

| Field | Required | Description |
|-------|----------|-------------|
| `name` | Yes | The variable name, used in templates as `{{ "{{" }} .Name {{ "}}" }}` |
| `description` | No | Human-readable description shown during interactive prompts |
| `type` | No | One of: `string`, `int`, `float`, `bool`, `list`, `map`, `enum`. Defaults to `string` |
| `default` | No | Default value if the user doesn't provide one |
| `options` | Enum only | List of allowed values for `enum` type |
| `order` | No | Integer controlling the order variables are prompted (lower = first) |
| `reference` | No | Name of another variable to reference for complex types |
| `validations` | No | List of validation rules |

## Variable Types

### `string`

The default type. Accepts any text value.

```yaml
- name: Name
  type: string
  default: "hello"
```

### `int`

An integer value.

```yaml
- name: Port
  type: int
  default: 8080
```

### `float`

A floating-point number.

```yaml
- name: Ratio
  type: float
  default: 0.75
```

### `bool`

A boolean (`true` or `false`).

```yaml
- name: EnableFeature
  type: bool
  default: true
```

### `list`

An ordered list of values. Provide via CLI as YAML syntax:

```yaml
- name: Tags
  type: list
  default:
    - tag1
    - tag2
```

CLI: `--var 'Tags=["web", "api"]'`

### `map`

A key-value map. Provide via CLI as YAML syntax:

```yaml
- name: Config
  type: map
  default:
    key1: value1
    key2: value2
```

CLI: `--var 'Config={host: "localhost", port: "8080"}'`

### `enum`

A constrained set of choices. The `options` field is required.

```yaml
- name: Environment
  type: enum
  options:
    - dev
    - staging
    - production
  default: dev
```

## Validations

Validations run in real-time during interactive prompts, showing which rules pass or fail as the user types.

| Validation | Description |
|-----------|-------------|
| `required` | Value cannot be empty |
| `length-{min}-{max}` | Character count must be within range (e.g., `length-3-50`) |
| `url` | Must be a valid URL |
| `email` | Must be a valid email address |
| `alpha` | English letters only |
| `digit` | Digits only |
| `countrycode2` | ISO 3166 Alpha-2 country code |
| `semver` | Semantic versioning format (e.g., `1.2.3`) |
| `regex(pattern)` | Custom regex pattern (e.g., `regex(^[a-z0-9-]+$)`) |

<Aside type="caution">
Regex patterns with spaces must be specified as YAML strings wrapped in quotes. Patterns with spaces cannot be provided as bare strings.
</Aside>

### Example with multiple validations

```yaml
- name: SlugName
  type: string
  description: URL-friendly project slug
  validations:
    - required
    - length-3-30
    - regex(^[a-z0-9-]+$)
```

## Variable References

For complex types (`list`, `map`), you can reference another variable instead of using Go template interpolation in defaults:

```yaml
variables:
  - name: Services
    type: list
    default:
      - api
      - web

  - name: PrimaryService
    type: string
    reference: Services  # References the Services list
```

## Variable Interpolation

Simple variable defaults can reference other variables using Go template syntax:

```yaml
variables:
  - name: AppName
    default: "my-app"

  - name: ContainerName
    default: "{{ "{{" }} .AppName {{ "}}" }}-container"
```

## Providing Values

Variables can be provided in multiple ways, with this precedence (highest to lowest):

1. `--var` CLI flags
2. Dependency-level `var_files`
3. `--var-file` CLI files
4. Dependency variable defaults
5. Root variable defaults in `boilerplate.yml`
6. Environment variables (`BOILERPLATE_VAR_<NAME>`)
7. Interactive prompts
